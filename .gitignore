# ── 开发过程截图与一次性探针（本地保留，不入库）──────────────
# _shot/ 是改版时对照用的真实像素图与探针脚本，共约 15M，
# 对复现页面没有作用，因此不进仓库。本地文件原样保留。
_shot/

# ── Python ────────────────────────────────────────────
__pycache__/
*.py[cod]
.venv/
venv/

# ── Node ──────────────────────────────────────────────
node_modules/

# ── 编辑器 / 系统 ──────────────────────────────────────
.vscode/
.idea/
.DS_Store
Thumbs.db
desktop.ini

# ── 临时文件 ───────────────────────────────────────────
*.tmp
*.bak
~$*
